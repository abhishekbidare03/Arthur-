# Ollama model blobs — multi-GB, never commit
models/

# Local database (conversation history)
data/

# Working docs — kept locally, deliberately not published.
# They record hardware specs, local paths and session-by-session reasoning,
# which belongs on this machine rather than in a public repo.
logs.md
phases.md

# Node
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# Voice model binaries (Phase 5)
bin/
voices/

# Generated by tools/make-icon.ps1 from frontend/logo/logo.png (Phase 6).
# Not committed: it is a derived binary that setup.bat rebuilds, and a 87 KB
# blob nobody can review in a diff is exactly what a build step is for.
assets/
frontend/public/favicon.ico

# Env
.env
.env.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
desktop.ini
